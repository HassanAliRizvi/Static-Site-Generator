from enum import Enum


class TextType(Enum):
	# declaring integer values for now
	NORMAL_TEXT = 1
	BOLD_TEXT = 2
	ITALIC_TEXT = 3
	CODE_TEXT = 4
	ANCHOR_TEXT = 5
	ALT_TEXT = 6

class TextNode(tex):
	self.text = text
	self.text_type = TextType.text_type
	self.url = url

	def __eq__(self,TextNode1()):
		return True if self.TextNode == TextNode() else False
	
	def __repr__()

